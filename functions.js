function tsFunction(){
    var h = "";
   var e = [" distribut blood to the body",
            "cells they act as a junction between", 
            "alteries and veins so they ", 
            "points of matreal exchange" ];
    e.forEach(tsFunction);
     document.getElementById("pra").innerHTML = h;
     function tsFunction(value, index, array) {
     h = h + value + "<br>"; 
  }
}
function ysFunction(){
    var h = "";
   var e = ["this is the factory which manufactures",
            "lots of proteins such as ezymes", 
            "it controls what type of materials should", 
            "circulate in the blood stream"];
    e.forEach(ysFunction);
     document.getElementById("pra").innerHTML = h;
     function ysFunction(value, index, array) {
     h = h + value + "<br>"; 
  }
}
function psFunction(){
    var t = "";
   var g = ["these are tubes which carry blood from",
            "heart rich in nutrients and oxygen except", 
            "pulmonary artery which carries blood", 
            "to the rungs to be oxyginated"];
    g.forEach(psFunction);
     document.getElementById("pra").innerHTML = t;
     function psFunction(value, index, array) {
     t = t + value + "<br>"; 
  }
}
function vsFunction(){
    var ax = "";
   var ay = ["these are tubes which carry blood to the",
            "heart poor in nutrients and oxygen except", 
            "pulmonary vein which carries blood", 
            "from the rungs full of oxygen to the heart"];
    ay.forEach(vsFunction);
     document.getElementById("pra").innerHTML = ax;
     function vsFunction(value, index, array) {
     ax = ax + value + "<br>"; 
  }
}
function gsFunction(){
    var ex = "";
   var ary = ["this is where alot af wast materials",
            "removed from the blood stream", 
            "such as urine which contains lots of ", 
            "useless and waste matrials"];
    ary.forEach(gsFunction);
     document.getElementById("pra").innerHTML = ex;
     function gsFunction(value, index, array) {
     ex = ex + value + "<br>"; 
  }
}